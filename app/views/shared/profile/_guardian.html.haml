%article.guardian
  %h4= guardian.full_name

  - if can? :administrate, guardian
    = render 'shared/profile/administrate_menu', profile: guardian, student: student

  - Guardian.fields.each do |field|
    = render 'shared/profile/field', field_data: field, field_value: guardian.send(field[:function])

  - if can? :update, guardian
    .edit
      = link_to "Edit profile", edit_guardian_path(guardian), class: "edit-link"