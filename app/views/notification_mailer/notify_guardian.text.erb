Dear <%= @notifications.guardian.name %>,

<% @notifications.students.each do |student| %>
<%= student[:student].name %>:

<% if student[:posts].present? %>
New posts:
<% student[:posts].each do |post| %>
<%= post.title %> - <%= post_url(post) %>
<% end %>
<% end %>

<% if student[:subjects].present? %>
Updated subjects:
<% student[:subjects].each do |subject| %>
<%= subject.name %> - <%= work_url(subject_id: subject.id, student_id: student[:student].id) %>
<% end %>
<% end %>

<% end %>

Thank you!

<%= root_url %>